import React from 'react';

import styles from './style.css';
import Card from "../UI/Card";

const Logo = props => {
    return (
        <div className={styles.logo}>
            <a href="#">Soban Jawaid</a>
        </div>
    )
}

export default Logo;